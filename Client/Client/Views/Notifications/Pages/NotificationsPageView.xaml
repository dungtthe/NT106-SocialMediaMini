<Page x:Class="Client.Views.Notifications.Pages.NotificationsPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Client.Views.Notifications.Pages"
      xmlns:comp="clr-namespace:Client.Views.Notifications.Components"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="450"
      Title="NotificationsPageView">

    <Page.Resources>
        <comp:BoolToColorConverter x:Key="BoolToColorConverter"/>
    </Page.Resources>

    <Grid Background="#F0F2F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" Padding="20" CornerRadius="10 10 0 0">
            <TextBlock Text="Thông báo" FontSize="24" FontWeight="Bold" Foreground="#1A73E8"/>
        </Border>

        <!-- Notifications List -->
        <Border Grid.Row="1" Margin="20,10,20,20" Background="White" CornerRadius="0 0 10 10">
            <ListBox x:Name="NotificationsListBox" Margin="10" Background="Transparent" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10" Margin="0,0,0,10" CornerRadius="8" Background="#FFFFFF">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="4" Opacity="0.2" ShadowDepth="1"/>
                            </Border.Effect>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Avatar -->
                                <Image Grid.Column="0" Width="40" Height="40" Source="{Binding sender.avatar}" VerticalAlignment="Top">
                                    <Image.Effect>
                                        <DropShadowEffect BlurRadius="5" Opacity="0.3" ShadowDepth="2"/>
                                    </Image.Effect>
                                </Image>

                                <!-- Content and Details -->
                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="{Binding content}" FontSize="14" Foreground="#333"/>
                                    <TextBlock Text="{Binding timestamp, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" FontSize="12" Foreground="#888" Margin="0,5,0,0"/>
                                    <TextBlock>
                                        <Hyperlink NavigateUri="{Binding link}" Click="Hyperlink_Click">
                                            <Run Text="Xem chi tiết" Foreground="#1A73E8" FontSize="12"/>
                                        </Hyperlink>
                                    </TextBlock>
                                </StackPanel>

                                <!-- Read Status Indicator -->
                                <Ellipse Width="10" Height="10" Fill="{Binding is_read, Converter={StaticResource BoolToColorConverter}}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,10,0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</Page>