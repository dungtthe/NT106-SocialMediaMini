<Page x:Class="Client.Views.Profile.Pages.ProfilePageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Client.Views.Profile.Pages"
      mc:Ignorable="d" 
      Width="500"
      Height="700">

    <Border Background="{DynamicResource ColorGreyOne}" CornerRadius="10" Margin="10">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header với Avatar -->
            <Border Grid.Row="0" Background="{DynamicResource ColorBlueOne}" CornerRadius="10 10 0 0" Padding="10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Ellipse Width="80" Height="80">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="/Resources/Images/meolag.jpg"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Nguyen Van A" FontSize="20" Foreground="{DynamicResource ColorWhiteOne}" FontWeight="Bold"/>
                        <TextBlock Text="Trạng thái: Online" FontSize="12" Foreground="{DynamicResource ColorWhiteOne}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Nội dung thông tin cá nhân -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="10">
                    <!-- Số điện thoại -->
                    <TextBlock Text="Số điện thoại:" FontSize="14" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="{DynamicResource ColorBlackTwo}"/>
                    <TextBox Text="0901234567" IsReadOnly="True" Background="{DynamicResource ColorGreyThree}" BorderThickness="0" Padding="5" Foreground="{DynamicResource ColorBlackOne}"/>

                    <!-- Địa chỉ -->
                    <TextBlock Text="Địa chỉ:" FontSize="14" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="{DynamicResource ColorBlackTwo}"/>
                    <TextBox Text="123 Đường ABC, Quận 1, TP.HCM" IsReadOnly="True" Background="{DynamicResource ColorGreyThree}" BorderThickness="0" Padding="5" Foreground="{DynamicResource ColorBlackOne}"/>

                    <!-- Giới tính -->
                    <TextBlock Text="Giới tính:" FontSize="14" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="{DynamicResource ColorBlackTwo}"/>
                    <TextBox Text="Nam" IsReadOnly="True" Background="{DynamicResource ColorGreyThree}" BorderThickness="0" Padding="5" Foreground="{DynamicResource ColorBlackOne}"/>

                    <!-- Ngày tháng năm sinh -->
                    <TextBlock Text="Ngày tháng năm sinh:" FontSize="14" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="{DynamicResource ColorBlackTwo}"/>
                    <TextBox Text="01/01/1990" IsReadOnly="True" Background="{DynamicResource ColorGreyThree}" BorderThickness="0" Padding="5" Foreground="{DynamicResource ColorBlackOne}"/>

                    <Rectangle Height="20"/>

                    <!-- Nút chỉnh sửa (tùy chọn) -->
                    <Button Content="Chỉnh sửa thông tin" HorizontalAlignment="Center" Width="200" Height="40" Background="{DynamicResource ColorBlueOne}" Foreground="{DynamicResource ColorWhiteOne}" Click="EditButton_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.8"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Page>