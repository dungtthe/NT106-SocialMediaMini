<UserControl
    x:Class="Client.Views.Posts.Components.PostItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="1588"
    mc:Ignorable="d">

    <Border
        Margin="0,0,0,15"
        Background="White"
        BorderBrush="{DynamicResource ColorBlueOne}"
        BorderThickness="1"
        CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect
                BlurRadius="5"
                Opacity="0.2"
                ShadowDepth="2" />
        </Border.Effect>

        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Header: Avatar, Username, Timestamp  -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Ellipse
                    Width="40"
                    Height="40"
                    Margin="0,0,10,0">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding User.Avatar}" />
                    </Ellipse.Fill>
                </Ellipse>

                <StackPanel Grid.Column="1">
                    <TextBlock
                        FontSize="16"
                        FontWeight="Bold"
                        Text="{Binding User.FullName}" />
                    <TextBlock
                        FontSize="12"
                        Foreground="Gray"
                        Text="{Binding CreateAt, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" />
                </StackPanel>
            </Grid>

            <!--  Blog Content  -->
            <TextBlock
                Grid.Row="1"
                Margin="0,0,0,10"
                FontSize="14"
                Text="{Binding Content}"
                TextWrapping="Wrap" />

            <!--  Images in post  -->
            <ItemsControl
                Grid.Row="1"
                Margin="0,40,0,10"
                ItemsSource="{Binding Images}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid
                            Width="200"
                            Height="200"
                            Margin="0,0,10,10">
                            <Border
                                Background="Transparent"
                                ClipToBounds="True"
                                CornerRadius="15">
                                <Image
                                    Width="200"
                                    Height="200"
                                    Source="{Binding}"
                                    Stretch="UniformToFill">
                                    <Image.Clip>
                                        <RectangleGeometry
                                            RadiusX="15"
                                            RadiusY="15"
                                            Rect="0,0,200,200" />
                                    </Image.Clip>
                                </Image>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <StackPanel
                Grid.Row="2"
                HorizontalAlignment="Left"
                Orientation="Horizontal">

                <!--  Like Button  -->
                <Button
                    Width="110"
                    Height="30"
                    Margin="0,0,10,0"
                    Background="Transparent"
                    BorderBrush="{DynamicResource ColorBlueOne}"
                    BorderThickness="1"
                    Cursor="Hand"
                    FontSize="12"
                    Foreground="{DynamicResource ColorBlueOne}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                                <StackPanel
                                    Margin="5,0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">
                                    <Image
                                        Width="14"
                                        Height="14"
                                        Margin="0,0,5,0"
                                        Source="/Resources/Icons/heart.png" />
                                    <TextBlock VerticalAlignment="Center" Text="{Binding Reactions.Count, StringFormat='({0})'}" />
                                </StackPanel>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource ColorBlueOne}" />
                                    <Setter Property="Foreground" Value="White" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!--  Comment Button  -->
                <Button
                    Width="120"
                    Height="30"
                    Background="Transparent"
                    BorderBrush="{DynamicResource ColorBlueOne}"
                    BorderThickness="1"
                    Cursor="Hand"
                    FontSize="12"
                    Foreground="{DynamicResource ColorBlueOne}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                                <StackPanel
                                    Margin="5,0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">
                                    <Image
                                        Width="14"
                                        Height="14"
                                        Margin="0,0,5,0"
                                        Source="/Resources/Icons/comment.png" />
                                    <TextBlock VerticalAlignment="Center" Text="{Binding CommentCount, StringFormat='({0})'}" />
                                </StackPanel>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource ColorBlueOne}" />
                                    <Setter Property="Foreground" Value="White" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

        </Grid>
    </Border>
</UserControl>