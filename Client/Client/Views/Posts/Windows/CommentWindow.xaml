<Window x:Class="Client.Views.Posts.Windows.CommentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        Title="Comments"
        Height="700" 
        Width="600"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource ColorWhiteOne}"
        ResizeMode="CanResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Post Content -->
        <Border Grid.Row="0" 
                Background="White"
                CornerRadius="8"
                BorderThickness="1"
                BorderBrush="{DynamicResource ColorBlueOne}"
                Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" 
                                 BlurRadius="5" 
                                 Opacity="0.2"/>
            </Border.Effect>
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header: Avatar, Username, Timestamp -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image Source="/Resources/Images/meolag.jpg" 
                           Width="40" 
                           Height="40" 
                           Margin="0,0,10,0"
                           RenderOptions.BitmapScalingMode="HighQuality"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="{Binding Username, RelativeSource={RelativeSource AncestorType=Window}}" 
                                   FontSize="16" 
                                   FontWeight="Bold"/>
                        <TextBlock Text="{Binding Timestamp, RelativeSource={RelativeSource AncestorType=Window}}" 
                                   FontSize="12" 
                                   Foreground="Gray"/>
                    </StackPanel>
                </Grid>

                <!-- Post Content -->
                <TextBlock Grid.Row="1" 
                           Text="{Binding Content, RelativeSource={RelativeSource AncestorType=Window}}" 
                           FontSize="14"
                           TextWrapping="Wrap"
                           Margin="0,0,0,10"/>
            </Grid>
        </Border>

        <!-- Comments List -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Margin="0,0,0,15">
            <StackPanel x:Name="CommentsPanel">
                <!-- Sample Comment 1 -->
                <Border Background="White"
                        CornerRadius="8"
                        BorderThickness="1"
                        BorderBrush="{DynamicResource ColorBlueOne}"
                        Margin="0,0,0,10"
                        Padding="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Image Source="/Resources/Images/meolag.jpg" 
                               Width="30" 
                               Height="30" 
                               Margin="0,0,10,0"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Jane Smith" 
                                       FontSize="14" 
                                       FontWeight="Bold"/>
                            <TextBlock Text="Great post! Thanks for sharing." 
                                       FontSize="12"
                                       TextWrapping="Wrap"/>
                            <TextBlock Text="12/05/2025 10:45" 
                                       FontSize="10" 
                                       Foreground="Gray"/>
                        </StackPanel>
                    </Grid>
                </Border>
                <!-- Sample Comment 2 -->
                <Border Background="White"
                        CornerRadius="8"
                        BorderThickness="1"
                        BorderBrush="{DynamicResource ColorBlueOne}"
                        Margin="0,0,0,10"
                        Padding="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Image Source="/Resources/Images/meolag.jpg" 
                               Width="30" 
                               Height="30" 
                               Margin="0,0,10,0"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Alex Johnson" 
                                       FontSize="14" 
                                       FontWeight="Bold"/>
                            <TextBlock Text="Really interesting thoughts!" 
                                       FontSize="12"
                                       TextWrapping="Wrap"/>
                            <TextBlock Text="12/05/2025 11:00" 
                                       FontSize="10" 
                                       Foreground="Gray"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Comment Input -->
        <Border Grid.Row="2" 
                Background="White"
                CornerRadius="8"
                BorderThickness="1"
                BorderBrush="{DynamicResource ColorBlueOne}">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" 
                                 BlurRadius="5" 
                                 Opacity="0.2"/>
            </Border.Effect>
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="CommentTextBox"
                         Grid.Column="0"
                         MinHeight="40"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         FontSize="14"
                         Padding="10"
                         BorderThickness="0"
                         Tag="Write a comment...">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Grid>
                                            <ScrollViewer x:Name="PART_ContentHost" 
                                                          VerticalAlignment="Top" 
                                                          Margin="0"/>
                                            <TextBlock Text="{TemplateBinding Tag}" 
                                                       FontStyle="Italic" 
                                                       Foreground="Gray" 
                                                       Margin="10,10,0,0" 
                                                       VerticalAlignment="Top" 
                                                       IsHitTestVisible="False">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <Button Grid.Column="1"
                        Content="Send"
                        Width="80"
                        Height="40"
                        FontSize="14"
                        Background="{DynamicResource ColorBlueOne}"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Margin="10,0,0,0"
                        Click="SendCommentButton_Click">
                    <Button.Effect>
                        <DropShadowEffect ShadowDepth="2" 
                                         BlurRadius="5" 
                                         Opacity="0.3"/>
                    </Button.Effect>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border CornerRadius="8" 
                                                Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.9"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="RenderTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>