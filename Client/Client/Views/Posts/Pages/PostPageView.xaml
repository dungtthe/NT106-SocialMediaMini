<Page
    x:Class="Client.Views.Posts.Pages.PostPageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:components="clr-namespace:Client.Views.Posts.Components"
    xmlns:converters="clr-namespace:Client.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:Client.Views.Posts.Pages"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="1628"
    Height="991"
    d:DesignHeight="991"
    d:DesignWidth="1628"
    mc:Ignorable="d">
    <!--  DataContext="{StaticResource PostViewModel}"  -->
    <Page.Resources>
        <!--  Style cho ScrollBar  -->
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ResourcesXAML/ScrollBar.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>




    <Grid Background="{DynamicResource ColorWhiteOne}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Blog Posting Area  -->
        <Border
            Grid.Row="0"
            Margin="20,20,20,10"
            Background="White"
            BorderBrush="{DynamicResource ColorBlueOne}"
            BorderThickness="1"
            CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect
                    BlurRadius="5"
                    Opacity="0.2"
                    ShadowDepth="2" />
            </Border.Effect>
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Avatar and TextBox  -->
                <StackPanel Orientation="Horizontal">
                    <!--  Avatar  -->
                    <Ellipse
                        Width="45"
                        Height="45"
                        Margin="11,0,11,30">
                        <Ellipse.Fill>
                            <ImageBrush x:Name="imgAvatar" />
                        </Ellipse.Fill>
                    </Ellipse>
                    <StackPanel Orientation="Horizontal">

                        <StackPanel MinWidth="1440" MaxWidth="1440">
                            <TextBox
                                x:Name="txtContentPost"
                                Height="auto"
                                materialDesign:HintAssist.Hint="Bạn đang nghĩ gì thế?"
                                materialDesign:TextFieldAssist.DecorationVisibility="Hidden"
                                AcceptsReturn="True"
                                BorderThickness="0"
                                FontSize="14"
                                Foreground="{DynamicResource ColorBlackOne}"
                                Text=""
                                TextChanged="txtContentPost_TextChanged"
                                TextWrapping="Wrap" />
                            <WrapPanel
                                x:Name="listImgSelect"
                                Height="auto"
                                Margin="0,10,0,0"
                                Orientation="Horizontal" />
                        </StackPanel>

                        <Image
                            x:Name="btnSelectImages"
                            Width="32"
                            Height="32"
                            Margin="20,0,0,0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Cursor="Hand"
                            MouseLeftButtonUp="btnSelectImages_MouseLeftButtonUp"
                            Source="/Resources/Icons/iconimg.png" />
                    </StackPanel>

                </StackPanel>
                <!--  Button  -->
                <StackPanel
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Orientation="Horizontal">
                    <Button
                        x:Name="btnHuyPost"
                        Grid.Row="1"
                        Width="100"
                        Height="35"
                        Margin="0,0,20,0"
                        HorizontalAlignment="Right"
                        Background="{DynamicResource ColorRedOne}"
                        BorderThickness="0"
                        Click="btnHuyPost_Click"
                        Content="Hủy"
                        Cursor="Hand"
                        FontSize="14"
                        Foreground="White">
                        <Button.Effect>
                            <DropShadowEffect
                                BlurRadius="5"
                                Opacity="0.3"
                                ShadowDepth="2" />
                        </Button.Effect>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.9" />
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <ScaleTransform ScaleX="0.98" ScaleY="0.98" />
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button
                        x:Name="btnDang"
                        Grid.Row="1"
                        Width="100"
                        Height="35"
                        HorizontalAlignment="Right"
                        Background="{DynamicResource ColorBlueOne}"
                        BorderThickness="0"
                        Content="Đăng"
                        Cursor="Hand"
                        FontSize="14"
                        Foreground="White">
                        <Button.Effect>
                            <DropShadowEffect
                                BlurRadius="5"
                                Opacity="0.3"
                                ShadowDepth="2" />
                        </Button.Effect>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.9" />
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <ScaleTransform ScaleX="0.98" ScaleY="0.98" />
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

            </Grid>
        </Border>
        <!--  Post List  -->
        <ScrollViewer
            Grid.Row="1"
            HorizontalScrollBarVisibility="Disabled"
            Style="{StaticResource ModernScrollViewerStyle}"
            VerticalScrollBarVisibility="Auto">
            <ItemsControl Margin="20,0,20,20" ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <components:PostItem Margin="0,0,0,20" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>